---
"@riconext/hermes-repo": minor
---

Add a local release workflow that versions with Changesets, updates the changelog, creates a matching git tag, and pushes the branch and tag for npm publishing.
